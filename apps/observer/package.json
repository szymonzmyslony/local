{
  "name": "@gallery-agents/observer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "cf:typegen": "wrangler types",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cloudflare/think": "0.15.1",
    "@gallery-agents/shared": "workspace:*",
    "@supabase/supabase-js": "2.112.2",
    "agents": "0.20.1",
    "ai": "7.0.58",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260810.1",
    "typescript": "^7.0.2",
    "vitest": "4.1.10",
    "wrangler": "^4.120.0"
  }
}
